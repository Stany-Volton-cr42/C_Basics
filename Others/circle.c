// this is the code for calculate the area of the circle
#include <stdio.h>
float const pi = 3.14;
int main(){
    int radius;
    printf("Enter the radious: ",radius);
    scanf("%d",&radius);
    printf("Area of circle %f",pi*(radius*radius));
    return 0;
}